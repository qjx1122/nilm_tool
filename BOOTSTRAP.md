# BOOTSTRAP.md — Agent 会话引导协议（NILM_AC）

> 用法：每次新 session 开头发一句「读 BOOTSTRAP.md 并按规则工作」即可。
> 本文件同时规定「开局恢复上下文」与「收尾固化上下文」，让思维链不依赖对话记忆。
> 核心约束：**会话纪要 / 专题报告一律写入指定文件、只追加不新建，避免文件爆炸、管理混乱。**

---

## 🟢 开局仪式（每个 session 开始时，自动执行，先做完再动手）

1. **拉取仓库现状**
   ```bash
   git status
   git log -10 --oneline
   git branch --show-current
   ```
2. **读取续接文件**
   - `cat STATUS.md`（若不存在，按文末模板创建骨架）
   - 可选回顾：`cat session/NILM_AC_session_complete.md`（历史会话纪要）、`cat REPORT_TEST.md`（专题报告）
3. **恢复开发环境**（session 切换后 bash 状态不保留）
   - 检查 GitHub 登录：`gh auth status` 或 `git ls-remote origin`
     - 失败则：用 SSH key（`~/.ssh` 会持久化），或重新 `gh auth login`
   - 依赖恢复：执行 `./setup.sh` 或 `pnpm install`
   - 对照 `README.md` 核对当前「安装 / 运行命令 / 数据目录 / 配置 / 输出产物 / 生产流程」是否与本地一致
4. **向用户汇报（3–5 行）**：当前分支 / 上次进度 / 本会话目标 / 任何阻塞

---

## 🔵 工作约定（每次都遵守）

- **决策要落盘**：任何「为什么这么做」的理由写进 `STATUS.md` 的「决策记录」，不要只留在对话里
- **小步提交**：每完成一个里程碑就 `git commit`，message 写清意图（这就是可恢复的"思考链"）
- **分支隔离**：不同任务用 `feature/xxx`、`fix/yyy`，切 session 直接 `git checkout` 回到现场
- **代码即事实**：以仓库 + 工作区文件为唯一事实来源，不靠对话记忆
- **文件治理**：会话纪要、专题报告只允许写入下列指定文件，**不新建额外文件**

---

## 🔴 收尾仪式（每次结束前，自动执行）

### 0. 更新 STATUS.md（必做）
更新：已完成 / 进行中 / 下一步 / 决策记录·踩坑 / 关键文件路径

### 1. 同步会话记录文件（必做）
向 `session/NILM_AC_session_complete.md` **追加**本次会话纪要（若 `session/` 目录不存在先创建）。
每条纪要结构：
```markdown
## [YYYY-MM-DD] 会话纪要
- 目标：
- 完成项：
- 关键决策：
- 未决问题：
- 相关文件/分支：
```

### 2. 按变化类型更新对应文档（条件触发）
> 仅当本次会话**确实发生**下列变化时才更新，无变化则跳过。

**→ 若发生以下任一类变化，更新 `README.md` 对应章节：**
- 安装 / 环境变化
- 运行命令变化
- 数据目录结构变化
- 配置文件结构变化
- 输出产物变化
- 生产推荐流程变化

**→ 若发生以下任一类变化，更新 `REPORT.md` 对应章节：**
- 算法路线发生重大变化
- 重大实验结论稳定
- KPI / 验收口径变化
- 发现重大工程事故或方法论教训
- 某个版本成为新的「推荐稳定版本」

### 3. 专题收尾同步（条件触发）
每完成一个 **用户专题 / 实验专题 / 验证专题**，向 `REPORT_TEST.md` **追加**该专题报告。
- ⚠️ **不新建文件**，所有专题报告统一沉淀在 `REPORT_TEST.md`，按专题分节追加
- 每条结构：
```markdown
## [YYYY-MM-DD] 专题：<专题名>
- 类型：用户专题 / 实验专题 / 验证专题
- 目标与假设：
- 方法 / 数据 / 参数：
- 结果 / 结论：
- 是否进入 REPORT.md（稳定结论）：是 / 否
- 遗留问题：
```

### 4. 提交并推送
```bash
git add -A
git commit -m "wip: <一句话说明本次进展>"
git push
```

### 5. 若要切 session
先完成 0–4 步再走，确保下一个 session 能无缝接上。

---

## 📄 STATUS.md 模板（首次创建时用）

```markdown
# STATUS.md
## 当前目标
-

## 已完成
- [ ]

## 进行中
-

## 下一步（TODO）
1.

## 决策记录 / 踩坑
-

## 关键文件路径
-
```
