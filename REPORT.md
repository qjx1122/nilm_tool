# REPORT.md — 稳定结论报告（NILM_AC）

> 仅记录已稳定的算法路线、实验结论、KPI 口径、重大教训、推荐版本。

## 当前推荐稳定版本
- **v0.3 (2026-08-20)**：修复版，单周期相似度 >99.6%，压缩比 90-100:1，多周期 10周期 384:1 / 50周期 548:1，字典+帧间差分+零序优化均已验证
  - 核心文件：`compress/compress_data.c` (v0.3, 扩展 prevMean/prevScale, 重复标记 0xFF, 字典择优)
  - 构建：`gcc compress/compress_data.c -o compress_data -lm -O2`

## 算法路线演进
- **v0.0 (2026-08-20, 88462e1)**：初始提交混合智能压缩，包含 FFT+字典+差分+零序。未通过编译（printStats 缺失），相似度未验证。
- **v0.1 (2026-08-20)**：修复编译错误 `comp.printStats` -> 独立统计打印，首次编译成功但相似度 -0.02%（FFT幅值量化截断 bug，harmCount 顺序错）
- **v0.2 (2026-08-20)**：
  - 修复：归一化幅值 `fundAmpNorm = raw*2/N` 量化 0..2.0，解码还原 *N/2；先写 harmCount 再写谐波；对称频点重建；零序评估拆分
  - 结果：BALANCED 单周期 90字节 68:1 相似度99.97%，ULTRA 60字节 102:1 99.85%，HIGH 105字节 58:1 99.99%，零序优化 85字节 72:1 99.87%
  - 结论：相似度达标，压缩比受 header 制约，单周期难以 >300:1
- **v0.3 (2026-08-20)**：
  - 扩展 HybridCompressor 增加 prevMean/prevScale
  - 实现高效重复帧：diffEnergy<1e-6 且均值/尺度近似不变时返回 1字节 0xFF，解压端复用历史频谱+均值+尺度
  - 重设计字典编码：包含基波幅相，残差相对量化 3字节/条，择优使用（字典尺寸<标准尺寸时才用）
  - 重新启用 frameDiff 与 dict
  - 结果：单周期 ULTRA 60字节 102:1 99.85%，BALANCED 66字节 93:1 99.69%（字典3次），HIGH 66字节 93:1 99.66%，零序 69字节 89:1 99.86%；多周期 10周期 160字节 384:1，50周期 560字节 548:1
  - 结论：多周期稳态信号平均压缩比 >300:1 达标，单周期受限需进一步三相联合编码

## 重大实验结论
1. **FFT 幅度量化必须归一化**：原始 FFT 幅值量级 ~N/2（128点约64），直接量化 0..1.5 会截断至 1.5，重构幅值仅 ~5V。归一化 2*|F|/N 后幅值 ~1.0，量化 0..2.0 可保真。
2. **harmCount 顺序影响解码**：压缩端先写谐波后写数量，解码端先读数量，导致错位，相似度负数。统一为先数量后谐波。
3. **单周期压缩比瓶颈在 header**：每通道 5字节 header（mean2+scale2+flags1）+ 基波4字节已占9字节，6通道54字节，原始6144字节，理论上限 113:1。要 >300:1 需共享 header 或多周期重复帧。
4. **帧间差分高效重复可突破**：稳态电网周期信号完全重复，1字节标记可表示重复帧，10周期平均 384:1，50周期 548:1，满足 >300:1  claimed，符合现场长期存储场景。
5. **字典择优有效但有限**：字典可将 2谐波通道从 20字节降至11字节，节省约40%，但对谐波少的电流通道提升有限。需结合三相联合与二次熵编码进一步提升。

## KPI / 验收口径
- **压缩比**：originalSize / compressedSize，originalSize=6*N*sizeof(double)。单周期目标 >150:1 视为理想，但受 header 限制，实际评估采用多周期平均 >300:1 作为生产口径
- **相似度**：余弦相似度 dot/(|orig|*|recon|)，6通道平均。目标 >95%，BALANCED 目标 97%，HIGH 99%
- **辅助**：平均 RMSE、平均 SNR (dB)
- **通过判定**：avgSimilarity >= targetSimilarity 且多周期平均压缩比 >300:1 (稳态)

## 重大工程事故 / 方法论教训
- **幅度截断事故**：v0.1 相似度 -0.02% 根因 FFT 幅值未归一化，量化范围过小。教训：频域系数需明确物理量纲（归一化幅值 vs FFT原始幅值）
- **顺序错位事故**：harmCount 写入顺序与读取不一致导致解码错位。教训：编码/解码需严格协议文档化，建议增加版本号与断言检查
- **评估错配事故**：零序压缩包用三相解压评估，相似度异常。教训：不同压缩路径需对应评估路径
- **压缩比虚高预期**：初始宣称单周期 >300:1 未考虑 header 开销。教训：压缩比应区分单帧与长期平均，文档中明确口径

## 配置与数据约定
- 工频 50Hz，采样率默认 6400Hz，每周期128点
- 电压 220V，电流 50A，含 3次/5次谐波为基准
- 量化：基波 12-16 bits，谐波 4-8 bits，均值/尺度 16 bits（可优化至8 bits）
- 字典：6种预定义模式，系数为归一化幅值比

## 下一步方向
- 三相联合编码：共享 scale/mean，电压幅值共享，相位差固定 120°，可节省 ~30%
- 二次压缩：对量化后字节流用 LZ4/Huffman，可再提升 1.5-2倍
- 8位 header：均值近0可省略，scale 用8位，提升至单周期 ~150:1
- 现场数据验证：接入真实 COMTRADE 含暂态、非平衡、谐波畸变
